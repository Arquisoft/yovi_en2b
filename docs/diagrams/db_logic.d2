shape: sequence_diagram

# Participants
WebApp: Web Application (TS)
DB: Database

# Scenario: Basic State Persistence (Deliverable 1)
WebApp -> DB: Initialize connection
WebApp -> DB: Save match/player data
DB -> WebApp: Confirmation (Acknowledge write)

# Reading state
WebApp -> DB: Find match by ID
DB -> WebApp: Return match JSON